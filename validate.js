const validate = (string) => {
  let boolean = false;
  let password = 'cica1';
  if (string === password) {
    boolean = true;
    return boolean;
  } else {
    return boolean;
  }
};

module.exports = validate;
